plugins {
    id 'java'
    id 'maven-publish'
    id 'non-core-module'
}

ext {
    artifactId = "konna.implementations-std-entity"
}

description = "Standard implementations of Konna Entity classes"

dependencies {

    implementation project(":component-entity")

}
