plugins {
    id 'java'
    id 'maven-publish'
    id 'non-core-module'
}

ext {
    artifactId = "konna.libfrontend-stb"
}

description = "Library frontend for stb libraries for using in Konna engine"
