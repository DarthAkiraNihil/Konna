plugins {
    id 'java'
    id 'maven-publish'
    id 'non-core-module'
}

ext {
    artifactId = "konna.libfrontend-dear-imgui"
}

description = "Library frontend for Dear ImGui for using in Konna engine (heavily based on SpaiR binding)"
