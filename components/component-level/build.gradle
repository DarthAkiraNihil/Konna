plugins {
    id 'java'
    id 'maven-publish'
    id 'non-core-module'
}

ext {
    artifactId = "konna.level"
}

description = "Level component of Konna engine"

dependencies {

    implementation project(":annotations-level")

    annotationProcessor project(":compiler-level")

}
